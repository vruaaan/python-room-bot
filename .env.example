#sample .env file
BOT_TOKEN=

# Only set this when deploying with webhooks (e.g. on Cloud Run).
# Leave blank for local development — the bot will fall back to long polling.
# Example: https://roombot-xxxx-as.a.run.app
WEBHOOK_URL=
